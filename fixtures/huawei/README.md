# Huawei Mock Fixtures

This directory will contain JSON fixture files for mock mode testing.

Files:
- `plant-summary.json` - Mock plant summary data
- `daily-series.json` - Mock daily energy series data
- `alarms.json` - Mock alarm data
